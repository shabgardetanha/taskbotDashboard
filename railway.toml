$schema = "https://railway.app/railway.schema.json"

[build]
builder = "RAILPACK"  # این کلیدی‌ترین خطه — Nixpacks رو bypass می‌کنه

[deploy]
startCommand = "npm start"

# Node 20 برای Next.js 14
[build]
nodeVersion = "20"