[build]
builder = "RAILPACK"

# این خط کلید موفقیت هست — cache رو غیرفعال می‌کنه تا EBUSY نده
[build.cache]
disable = true

[deploy]
startCommand = "npm start"

# SSL Certificate Configuration
# supabase-ca.crt is automatically available since it's committed to git
# Railway deploys from git, so all committed files are available at runtime
